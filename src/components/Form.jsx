import styled from "styled-components";

const FormContainer = styled.div`


`

const Form = () => {

  return (
    <FormContainer>
      <form>
        <h1>Form</h1>
      </form>
    </FormContainer>
  );
}

export default Form;
